import { css } from 'lit';

const styles = css`
  :host {
    align-items: center;
    display: flex;
    height: fit-content;
    justify-content: center;
    width: fit-content;
  }

  svg {
    height: 100%;
    width: 100%;
    fill: currentColor;
  }
`;

export { styles };
